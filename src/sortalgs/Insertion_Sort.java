package sortalgs;
/* Insertion Sort is a basic sorting algorythm. It goes through the array and reads all the elements
 * Then is remembers wich one was the smalles and puts that at the begin of the array. the next smalles element would go
 * on the right, next to it. This way it creates a sorted part of the array at the beginning, wich allows it to skip that 
 * part on each loop.
 */
public class Insertion_Sort {
public static void insertionSort(Integer[] array) {

}
}
