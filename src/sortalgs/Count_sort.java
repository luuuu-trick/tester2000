package sortalgs;
/* Count sort is different from a lot of other sorting algorythms because it operates differently
 * Count sort creates a second array wich counts how often each number was present in the array. then it lists
 * all elements the number of times they have been counted in the output array. 
 * Count sort is very efficient if there are a lot of elements that are of equal size.
 * 
 */
public class Count_sort {
    public static void countsort(Integer[] array){
        //return;
    }
}
